import { Link } from 'expo-router'; import React,{useState} from 'react'; import { Alert,KeyboardAvoidingView,Platform,StyleSheet,Text,TextInput } from 'react-native'; import { Button,ui } from '../../src/components/Ui'; import { useAuth } from '../../src/contexts/AuthContext';
export default function Login(){const {login}=useAuth();const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [busy,setBusy]=useState(false);async function go(){try{setBusy(true);await login(email,password)}catch(e:any){Alert.alert('Não foi possível entrar',e?.response?.data?.message||e.message)}finally{setBusy(false)}}return <KeyboardAvoidingView behavior={Platform.OS==='ios'?'padding':undefined} style={[ui.page,{justifyContent:'center'}]}><Text style={ui.title}>SchoolGo</Text><Text style={ui.subtitle}>Segurança e acompanhamento do transporte escolar.</Text><TextInput style={s.input} placeholder="E-mail" autoCapitalize="none" keyboardType="email-address" value={email} onChangeText={setEmail}/><TextInput style={s.input} placeholder="Senha" secureTextEntry value={password} onChangeText={setPassword}/><Button title={busy?'Entrando...':'Entrar'} onPress={go} disabled={busy}/><Link href="/(auth)/register" style={s.link}>Criar uma conta</Link></KeyboardAvoidingView>}
const s=StyleSheet.create({input:{backgroundColor:'#fff',borderRadius:12,padding:14,marginBottom:10,borderWidth:1,borderColor:'#E4E9F0'},link:{textAlign:'center',marginTop:20,color:'#246BFD',fontWeight:'700'}});
